apply plugin: "java"

dependencies {
    compileOnly "com.github.GiantLuigi4:FlameLoader:1.0"
}

sourceCompatibility = 1.8
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets.main.java.srcDirs = [ "src/" ]

eclipse.project.name = appName + "-core"
